{
  "name": "asmltr-core",
  "version": "0.1.0",
  "private": true,
  "description": "asmltr unified channel-agnostic agent backend (local Agent SDK, Max subscription)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/runner.js && node --check src/moderation.js && node --check src/resolver.js && node --check src/sessions.js && node --check src/envelope.js && node --check src/emitter.js"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.209",
    "better-sqlite3": "^11.10.0",
    "express": "^4.22.2",
    "openai": "^6.0.0",
    "uuid": "^9.0.1"
  }
}
